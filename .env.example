# 复制本文件为 .env 后填写（.env 不会被 git 跟踪）

# ===== 安全密钥 =====
# 桌面端：首次启动会自动生成一段随机密钥写入 %APPDATA%/Jingent/.env，无需手填。
# 服务端部署：必须显式设置成一段高强度随机字符串（可用 python -c "import secrets;print(secrets.token_urlsafe(32))" 生成）。
SECRET_KEY=
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ===== AI 大模型（OpenAI 兼容协议，任选一家）=====
# 详细接入步骤见网页右上角「接入教程」按钮。
#
# 【DeepSeek（推荐）】
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
LLM_API_KEY=sk-your-api-key
#
# 【阿里云百炼 / 通义】
# LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# LLM_MODEL=qwen-plus
# LLM_API_KEY=sk-your-api-key
#
# 【本地 Ollama（免费）】
# LLM_BASE_URL=http://localhost:11434/v1
# LLM_MODEL=qwen2.5
# LLM_API_KEY=ollama

# ===== 数据库 =====
# 留空/不设置 = 桌面端默认，数据落在 %APPDATA%/Jingent/app.db（装到 Program Files 也可写）。
# ⚠️ 不要在这里写 sqlite:///./app.db —— 那会覆盖上面的默认值，导致安装版因目录不可写而启动失败。
#
# 服务端部署时按需显式指定：
# SQLALCHEMY_DATABASE_URL=sqlite:////var/lib/jingent/app.db
# 如需 MySQL（并安装 pymysql）：
# SQLALCHEMY_DATABASE_URL=mysql+pymysql://root:password@localhost:3306/ai_rove

# ===== 未登录用户聊天限流：每个 IP 每天最多免费体验多少次（跨自然日自动重置）=====
ANONYMOUS_RATE_LIMIT_MAX=10

# ===== 后台管理员（访问 /admin 登录用）=====
# 密码务必改成强密码；留空则后台登录一律被拒绝。建议给 /admin 配 HTTPS。
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
